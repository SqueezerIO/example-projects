- description: Installing project's npm packages
  bin: cd ${project.path} && npm install --silent
- description: Installing required tools
  bin: ${sudo} npm install webpack node-sass phantomjs-prebuilt selenium-standalone webdriverio wdio-mocha-framework wdio-spec-reporter  -g --silent
- description: Installing Selenium webdriver
  bin: ${sudo} selenium-standalone install
