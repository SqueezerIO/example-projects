'use strict';

import vars from '../vars';
import DB from '../../../../lib/db';

const db = new DB(vars);

class Database {
  constructor(vars) {
    this.vars = vars;
  }

  addProduct() {
    
  }
}

export default Database;