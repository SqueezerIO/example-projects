- description: Installing project's packages
  bin: cd ${project.path} && ${project.path}/node_modules/.bin/yarn
  args:
    - 'install'
    - '--prefer-offline'
    - '--non-interactive'
    - '--no-progress'
    - '--silent'