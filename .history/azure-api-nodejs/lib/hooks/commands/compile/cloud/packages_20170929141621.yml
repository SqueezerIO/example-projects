- description: Adding dependencies
  bin: cd ${output} && ${project.path}/node_modules/.bin/yarn
  args:
    - 'add'
    - '${packages}'
    - '--prefer-offline'
    - '--non-interactive'
    - '--no-progress'
    - '--silent'