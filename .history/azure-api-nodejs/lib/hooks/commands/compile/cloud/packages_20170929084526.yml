- description: Adding dependencies
  bin: cd ${project.path} && ${project.path}/node_modules/.bin/yarn add ${packages} --prefer-offline --non-interactive --no-progress -s
  args:
    - 'add'
    - '${packages}'
    - '--prefer-offline'
    - '--non-interactive'
    - '--no-progress'
    - '--silent'
    - '--modules-folder'
    - '${output}/node_modules'